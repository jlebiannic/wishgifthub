/api/users/me/avatar:
  put:
    tags:
      - Utilisateurs
    summary: Mettre à jour l'avatar de l'utilisateur
    description: |
      Permet à un utilisateur authentifié de modifier son avatar.
      L'avatar sélectionné doit être l'un des avatars prédéfinis disponibles.
    operationId: updateUserAvatar
    security:
      - bearerAuth: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../schemas/requests.yml#/UpdateAvatarRequest'
    responses:
      '200':
        description: Avatar mis à jour avec succès
        content:
          application/json:
            schema:
              $ref: '../schemas/responses.yml#/UserResponse'
      '400':
        description: Requête invalide (avatar ID invalide)
        content:
          application/json:
            schema:
              $ref: '../schemas/errors.yml#/components/schemas/ErrorResponse'
      '401':
        description: Non authentifié
        content:
          application/json:
            schema:
              $ref: '../schemas/errors.yml#/components/schemas/ErrorResponse'

