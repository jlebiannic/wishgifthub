# Configuration de l'API Backend
# En développement, laisser vide pour utiliser le proxy Vite configuré dans vite.config.ts
# En production, définir l'URL complète de l'API (ex: https://api.votre-domaine.com)
VITE_API_URL=

# Le proxy est configuré dans vite.config.ts
# Par défaut en dev : http://localhost:8080 via proxy /api
# server.proxy['/api'].target = 'http://localhost:8080'

# Autres variables d'environnement (si nécessaire)
# VITE_APP_TITLE=WishGiftHub

